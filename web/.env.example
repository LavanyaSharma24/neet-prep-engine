# Copy to .env.local for local development.
# Base URL of the api/ backend's /escalate endpoint (no trailing slash).
# Only used when local retrieval confidence is below threshold AND the
# browser is online — see src/api.ts.
VITE_API_BASE_URL=http://localhost:8000
